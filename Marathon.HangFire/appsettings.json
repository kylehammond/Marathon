{
  "Environment": "Default",
  "LoopTimes": 5,
  "Serilog": {
    "MinimumLevel": {
      "Default": "Information",
      "Override": {
        "Micrsoft": "Information", // if namespace MS, log info level or higher
        "System": "Warning" // if namespace System, log warning or higher
      }
    }
  }
}